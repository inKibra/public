# @inkibra/router

Type-safe routing with query DSL, mutations, event streaming, loaders, React hooks, and HMR support built on api-base. Provides a complete transport layer for both core and React-based route handling with Result types for error management.

## Install

```bash
bun add @inkibra/router
```

## License

MIT
