/**
 * HTTP methods for API routes
 *
 * Values are uppercase to match the HTTP specification and
 * the Web API's Request.method property.
 */
export enum HttpMethod {
  GET = 'GET',
  POST = 'POST',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
  PUT = 'PUT',
}
